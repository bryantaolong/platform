<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bryan.platform.mapper.post.PostMapper">

    <sql id="BaseColumn">
        id,user_id,title,content,status,category_id,tags,comment_area_status,
        view_count,like_count,comment_count,collect_count,share_count,weight,
        deleted,version,created_at,updated_at,created_by,updated_by
    </sql>

    <resultMap id="BaseResultMap" type="com.bryan.platform.domain.entity.post.Post">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="category_id" property="categoryId"/>
        <result column="tags" property="tags" typeHandler="com.bryan.platform.handler.PostgreSqlTextArrayTypeHandler"/>
        <result column="comment_area_status" property="commentAreaStatus"/>
        <result column="view_count" property="viewCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="collect_count" property="collectCount"/>
        <result column="share_count" property="shareCount"/>
        <result column="weight" property="weight"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
    </resultMap>

    <!-- 1. 单条插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post (
            user_id,title,content,status,category_id,tags,comment_area_status,
            created_at,updated_at,created_by,updated_by
        ) VALUES (
                     #{userId},#{title},#{content},#{status},#{categoryId},#{tags,typeHandler=com.bryan.platform.handler.PostgreSqlTextArrayTypeHandler},#{commentAreaStatus},
                     #{createdAt},#{updatedAt},#{createdBy},#{updatedBy}
                 )
    </insert>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE post
        SET deleted = 1,
            version = version + 1
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <!-- 整行更新（乐观锁） -->
    <update id="update">
        UPDATE post
        SET title = #{title},
            content = #{content},
            status = #{status},
            category_id = #{categoryId},
            tags = #{tags,typeHandler=com.bryan.platform.handler.PostgreSqlTextArrayTypeHandler},
            comment_area_status = #{commentAreaStatus},
            weight = #{weight},
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
          AND deleted = 0
    </update>

    <!-- 单条查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 批量 IDS 查询 -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </select>

    <!-- 用户分页 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE user_id = #{userId}
        AND deleted = 0
        ORDER BY created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <!-- 全局分页 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE deleted = 0
        ORDER BY weight DESC, created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <!-- 计数 -->
    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM post
        WHERE user_id = #{userId}
          AND deleted = 0
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM post
        WHERE deleted = 0
    </select>

    <!-- 搜索分页 -->
    <select id="selectBySearch" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND created_by LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="tags != null and tags != ''">
                AND tags LIKE CONCAT('%', #{tags}, '%')
            </if>
        </where>
        ORDER BY weight DESC, created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <!-- 搜索计数 -->
    <select id="countBySearch" resultType="long">
        SELECT COUNT(*)
        FROM post
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND created_by LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="tags != null and tags != ''">
                AND tags LIKE CONCAT('%', #{tags}, '%')
            </if>
        </where>
    </select>

</mapper>