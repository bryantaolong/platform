<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bryan.platform.mapper.user.UserFollowMapper">

    <resultMap id="BaseResultMap" type="com.bryan.platform.domain.entity.user.UserFollow">
        <id column="id" property="id"/>
        <result column="follower_id"  property="followerId"/>
        <result column="following_id" property="followingId"/>
        <result column="deleted"      property="deleted"/>
        <result column="version"      property="version"/>
        <result column="created_at"   property="createdAt"/>
        <result column="updated_at"   property="updatedAt"/>
        <result column="created_by"   property="createdBy"/>
        <result column="updated_by"   property="updatedBy"/>
    </resultMap>

    <sql id="BaseColumns">
        id, follower_id, following_id, deleted, version,
        created_at, updated_at, created_by, updated_by
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_follow (
            follower_id, following_id, deleted, version,
            created_at, updated_at, created_by, updated_by
        ) VALUES (
                     #{followerId}, #{followingId}, #{deleted}, #{version},
                     #{createdAt}, #{updatedAt}, #{createdBy}, #{updatedBy}
                 )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_follow
        WHERE id = #{id}
    </select>

    <select id="selectPageByFollowerId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_follow
        WHERE follower_id = #{followerId}
        AND deleted = 0
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectPageByFollowingId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_follow
        WHERE following_id = #{followingId}
        AND deleted = 0
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByFollowerId" resultType="long">
        SELECT COUNT(*)
        FROM user_follow
        WHERE follower_id = #{followerId}
          AND deleted = 0
    </select>

    <select id="countByFollowingId" resultType="long">
        SELECT COUNT(*)
        FROM user_follow
        WHERE following_id = #{followingId}
          AND deleted = 0
    </select>

    <select id="countByFollowerIdAndFollowingId" resultType="long">
        SELECT COUNT(*)
        FROM user_follow
        WHERE follower_id = #{followerId}
          AND following_id = #{followingId}
          AND deleted = 0
    </select>

    <update id="updateDeletedByFollowerIdAndFollowingId">
        UPDATE user_follow
        SET deleted = #{deleted},
            version = #{version},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE follower_id = #{followerId}
          AND following_id = #{followingId}
    </update>

    <select id="selectAllByFollowerId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_follow
        WHERE follower_id = #{followerId}
        AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="selectFollowingIdsByFollowerId" resultType="java.lang.Long">
        SELECT following_id
        FROM user_follow
        WHERE follower_id = #{followerId}
        AND deleted = 0
        ORDER BY created_at DESC
    </select>

</mapper>