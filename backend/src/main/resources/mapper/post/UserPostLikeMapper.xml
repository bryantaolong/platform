<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bryan.platform.mapper.post.UserPostLikeMapper">

    <sql id="BaseColumn">
        id,user_id,post_id,deleted,version,created_at,updated_at,created_by,updated_by
    </sql>

    <resultMap id="BaseResultMap" type="com.bryan.platform.domain.entity.post.UserPostLike">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="post_id" property="postId"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_post_like (
            user_id, post_id, deleted, version,
            created_at, updated_at, created_by, updated_by
        ) VALUES (
                     #{userId}, #{postId}, #{deleted}, #{version},
                     #{createdAt}, #{updatedAt}, #{createdBy}, #{updatedBy}
                 )
    </insert>

    <update id="deleteById">
        UPDATE user_post_like
        SET deleted = 1,
            version = #{version} + 1,
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE id = #{id}
          AND deleted = 0
          AND version = #{version}
    </update>

    <update id="deleteByUserIdAndPostId">
        UPDATE user_post_like
        SET deleted = 1,
            version = #{version} + 1,
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE user_id = #{userId}
          AND post_id = #{postId}
          AND deleted = 0
          AND version = #{version}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM user_post_like
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByUserIdAndPostId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM user_post_like
        WHERE user_id = #{userId}
        AND post_id = #{postId}
        AND deleted = 0
    </select>

    <select id="selectByUserIdAndPostIdIncludeDeleted" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM user_post_like
        WHERE user_id = #{userId}
        AND post_id = #{postId}
    </select>

    <select id="existsByUserIdAndPostId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user_post_like
        WHERE user_id = #{userId}
          AND post_id = #{postId}
          AND deleted = 0
    </select>

    <select id="existsByUserIdAndPostIdIncludeDeleted" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user_post_like
        WHERE user_id = #{userId}
          AND post_id = #{postId}
    </select>

    <update id="restoreLike">
        UPDATE user_post_like
        SET deleted = 0,
            version = #{version} + 1,
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE user_id = #{userId}
          AND post_id = #{postId}
          AND version = #{version}
    </update>

    <!-- 推荐相关：查询用户点赞的帖子ID列表 -->
    <select id="selectPostIdsByUserId" resultType="java.lang.Long">
        SELECT post_id
        FROM user_post_like
        WHERE user_id = #{userId}
        AND deleted = 0
        ORDER BY created_at DESC
    </select>

</mapper>