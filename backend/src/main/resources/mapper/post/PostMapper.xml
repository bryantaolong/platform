<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bryan.platform.mapper.post.PostMapper">

    <sql id="BaseColumn">
        id,user_id,title,content,status,category_id,tags,comment_area_status,
        view_count,like_count,comment_count,collect_count,share_count,weight,
        deleted,version,created_at,updated_at,created_by,updated_by
    </sql>

    <resultMap id="BaseResultMap" type="com.bryan.platform.domain.entity.post.Post">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="category_id" property="categoryId"/>
        <result column="tags" property="tags"
                typeHandler="com.bryan.platform.handler.PostgreSqlTextArrayTypeHandler"/>
        <result column="comment_area_status" property="commentAreaStatus"/>
        <result column="view_count" property="viewCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="collect_count" property="collectCount"/>
        <result column="share_count" property="shareCount"/>
        <result column="weight" property="weight"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post (
            user_id, title, content, status, category_id, tags, comment_area_status,
            deleted, version, created_at, updated_at, created_by, updated_by
        ) VALUES (
                     #{userId}, #{title}, #{content}, #{status}, #{categoryId},
                     #{tags,typeHandler=com.bryan.platform.handler.PostgreSqlTextArrayTypeHandler}, #{commentAreaStatus},
                     #{deleted}, #{version}, #{createdAt}, #{updatedAt}, #{createdBy}, #{updatedBy}
                 )
    </insert>

    <update id="deleteById">
        UPDATE post
        SET deleted = 1,
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="update">
        UPDATE post
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="tags != null">tags = #{tags,typeHandler=com.bryan.platform.handler.PostgreSqlTextArrayTypeHandler},</if>
            <if test="commentAreaStatus != null">comment_area_status = #{commentAreaStatus},</if>
            <if test="weight != null">weight = #{weight},</if>
            version = #{version},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        </set>
        WHERE id = #{id}
        AND deleted = 0
    </update>

    <update id="updateLikeCount">
        UPDATE post
        SET like_count = like_count + #{delta},
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="updateCollectCount">
        UPDATE post
        SET collect_count = collect_count + #{delta},
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="updateViewCount">
        UPDATE post
        SET view_count = view_count + #{delta},
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="updateCommentCount">
        UPDATE post
        SET comment_count = comment_count + #{delta},
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE user_id = #{userId}
        AND deleted = 0
        ORDER BY created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectByUserIdAndStatus" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE user_id = #{userId}
        AND status = #{status}
        AND deleted = 0
        ORDER BY created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE deleted = 0
        ORDER BY weight DESC, created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM post
        WHERE user_id = #{userId}
          AND deleted = 0
    </select>

    <select id="selectPageByStatus" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE status = #{status}
        AND deleted = 0
        ORDER BY weight DESC, created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*)
        FROM post
        WHERE status = #{status}
          AND deleted = 0
    </select>

    <select id="countByUserIdAndStatus" resultType="long">
        SELECT COUNT(*)
        FROM post
        WHERE user_id = #{userId}
          AND status = #{status}
          AND deleted = 0
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM post
        WHERE deleted = 0
    </select>

    <select id="selectByTitle" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            AND status = #{status}
        </where>
        ORDER BY weight DESC, created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectBySearch" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND created_by LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="tags != null and tags != ''">
                AND tags LIKE CONCAT('%', #{tags}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY weight DESC, created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByTitle" resultType="long">
        SELECT COUNT(*)
        FROM post
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="countBySearch" resultType="long">
        SELECT COUNT(*)
        FROM post
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND created_by LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="tags != null and tags != ''">
                AND tags LIKE CONCAT('%', #{tags}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectRecentPosts" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE status = 0
          AND deleted = 0
          AND created_at >= NOW() - (#{hours} * INTERVAL '1 HOUR')
        ORDER BY weight DESC, created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectByTags" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE status = 'PUBLISHED'
        AND deleted = 0
        AND tags &amp;&amp; #{tags,typeHandler=com.bryan.platform.handler.PostgreSqlTextArrayTypeHandler}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectByUserIds" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        AND status = 'PUBLISHED'
        AND deleted = 0
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectHotPosts" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumn"/>
        FROM post
        WHERE status = 'PUBLISHED'
        AND deleted = 0
        AND created_at >= NOW() - INTERVAL '7 DAYS'
        ORDER BY (view_count * 0.1 + like_count * 3 + comment_count * 10) DESC
        LIMIT #{limit}
    </select>

    <!-- 查询所有未被删除的文章内容（用于僵尸图片清理任务） -->
    <select id="selectAllPostContents" resultType="string">
        SELECT content
        FROM post
        WHERE deleted = 0
          AND content IS NOT NULL
          AND content != ''
    </select>
</mapper>